# Backend API (Railway / local FastAPI)
API_URL=http://localhost:8000

# Public URL for client-side references (optional)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Direct backend SSE (recommended) — avoids Next.js proxy buffering chat tokens
NEXT_PUBLIC_API_URL=http://localhost:8000
